<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Your Therapy Notebook</title>
</head>
<body>
  <%- include('../partials/_navbar.ejs') %>
  <h1>Your Therapy Notebook</h1>
  <a href="/users/<%= user._id %>/triggers/new">Add an Event that Triggered You</a>
  <ul>
    <% triggers.forEach(trigger => { %>
      <li>
        <a href="/users/<%= user._id %>/triggers/<%= trigger._id %>">
        <%= trigger.date %> at <%= trigger.location %>
      </li>
    <% }) %>
  </ul>
</body>
</html>
