<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= trigger.location %></title>
  <link rel="stylesheet" href="/css/show.css">
</head>
<body>
  <%- include('../partials/_navbar.ejs') %>
  <div class="show-body">
      <h1><%= trigger.title %> at <%= trigger.location %></h1>
      <% if (trigger.notes) { %>
        <p>Notes: <%= trigger.notes %></p>
      <% } %>
    </div>
      <div class="show-actions">
        <a class="action-button" href="/users/<%= user._id %>/triggers/<%= trigger._id %>/edit">
          Edit this Event
        </a>
        <form action="/users/<%= user._id %>/triggers/<%= trigger._id %>?_method=DELETE" method="POST">
          <button type="submit" class="action-button">Delete this Trigger</button>
          <p>Current Status: <%= trigger.status %></p>
        </form>
      </div>
  </div>
</body>
</html>
