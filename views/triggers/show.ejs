<!-- views/applications/show.ejs -->

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= trigger.location %></title>
</head>
<body>
  <%- include('../partials/_navbar.ejs') %>
  <h1><%= trigger.location %></h1>
  <!-- <% if (application.notes) { %>
    <p>Notes: <%= application.notes %></p>
  <% } %>
  <% if (application.postingLink) { %>
    <a href="<%= application.postingLink %>">Visit this job posting</a>
  <% } %>
  <p>The status of this application is: <%= application.status %></p>

  Add the following link -->
  <a href="/users/<%= user._id %>/triggers/<%= trigger._id %>/edit">
    Edit this application
  </a>
  <!--  -->

  <form
    action="/users/<%= user._id %>/triggers/<%= trigger._id %>?_method=DELETE"
    method="POST"
  >
    <button type="submit">Delete this application</button>
  </form>
</body>
</html>
